import {useState} from "react"

export default function UseForm ({onCreate}) {
    const [nuevoUsuario, setNuevoUsuario] = useState ({name: "", email, "" })

    function onSubmit(event) {
        event.preventDefault({
            
        })

    }
}